//  hoisting;
// two brought category-  prem ative and non premative
// primitive data types in javascript example
// Diffent null and undifine

// Premative - Number , String, Boolean , null, undifine
// non premative - Array and object

// const nums = (1, 2, 3, 4, 5);
// console.log(Math.min(nums));

// let n = Math.floor(Math.random() * 10);
// console.log("name".toLocaleUpperCase());

// All number except 0 and all the string except empty string  and  Boolean false comes under category Falsy value

// Coercion In javascript ------------

// Operators
//   Arethmatict operators +, -, *, /, %, **
//   comparitin oparetor ==, ===, <, >, <=, >=, !=, !==
//   logical operator &&, ||
//   Ternari operator condition ? true : false

// Condition

// if (condition) {

// } else {

// }

// let value = 0;
// // while (value <= 5) {
// //   console.log(`${value}`);
// //   value++;
// // }

// do {
//   console.log(`${value}`);
//   value++;
// } while (value <= 5);

// let names = ["Deepak", "Anurag", "Shivam"];
// let upperNames = [];

// for (let i = 0; i < names.length; i++) {
//   upperNames.push(names[i].toLocaleUpperCase());
// }
// console.log(upperNames);

// Array methods
// concat()
// indexOf(),
// lastindexof(),
// includes(),
// isArray(),
// toString(),
// join(),
// slice(),
// splice(),
// remove (),
// toString(),
// push(),
// pop(),
// shift(),
// unshift(),
// reverse(),
// sort(),
// log(),
// include()

// function user() {
//   console.log("Deepak");
// }

// user()

// Hoisting, scop, excicuting context, call satck,
